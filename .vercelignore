# Build artifacts
dist
build

# Dependencies
node_modules

# OS files
.DS_Store

# Logs & caches
*.log
.vite
coverage

# Tests
src/test
vitest.config.ts

# Supabase migrations & dev config (server-managed)
supabase

# NOTE: .env is intentionally NOT ignored.
# Lovable Cloud manages it with the public Supabase URL and publishable (anon) key,
# both of which are required at Vite build time and safe to commit (RLS-protected).
